// This file is part of a legacy route structure and has been intentionally disabled 
// to prevent build errors. The active layout is under /src/app/[locale]/.
import React from 'react';

export default function DisabledLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
